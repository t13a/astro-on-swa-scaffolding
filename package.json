{
  "name": "astro-on-swa-scaffolding",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -r 'npm run watch -w api' 'swa start'",
    "build": "npm run build -ws",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "packages/api",
    "packages/ui"
  ],
  "devDependencies": {
    "@azure/static-web-apps-cli": "^2.0.8",
    "concurrently": "^9.2.1"
  }
}
